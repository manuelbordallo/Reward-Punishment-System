# Nixpacks configuration for Railway frontend deployment
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd reward-punishment-web && npm ci"]

[phases.build]
cmds = ["cd reward-punishment-web && npm run build"]

[start]
cmd = "cd reward-punishment-web && npx serve -s build -l $PORT"